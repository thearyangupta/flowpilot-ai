"""FlowPilot Streamlit presentation package.

The UI is a thin client of the FlowPilot FastAPI application.

Business rules, persistence, authorization, workflow state transitions,
provider interactions, and other durable application behavior belong to
the backend and must be accessed through its HTTP API.
"""
